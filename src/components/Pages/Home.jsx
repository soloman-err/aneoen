import React from 'react';
import Carousel from '../Carousel/Carousel';

const HomeLayout = () => {
    return (
        <div>
            <Carousel></Carousel>
        </div>
    );
};

export default HomeLayout;